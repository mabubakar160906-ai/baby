# Portfolio (JSX)

Plain JavaScript (JSX) version of the portfolio — Vite + React + Tailwind CSS, no TypeScript.

## Quick start

```bash
npm install
npm run dev
```

Then open http://localhost:5173

## Build

```bash
npm run build
npm run preview
```
